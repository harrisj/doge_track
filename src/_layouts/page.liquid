---
layout: default
---
<div class="md:container md:mx-auto px-8">
  {% render 'header_bar', metadata: site.metadata %}
  <main class="my-text flex flex-col min-h-screen">
    {{ content }}
  </main>
  {% render 'footer', metadata: site.metadata %}
</div>
