---
layout: default
---
{% urls = site.data.page_listing.sections.map {|s| s.pages }.flatten %}

{% page_index = urls.find_index {|x| x.url.gsub(/\/$/, '') == resource.relative_url.gsub(/\/$/, '') }
  if page_index
    prev_pg = urls[page_index-1]
    next_pg = urls[page_index+1]
  else
    prev_pg = nil
    next_pg = nil
  end
%}

<div class="drawer">
  <input id="my-drawer-3" type="checkbox" class="drawer-toggle" />
  <div class="drawer-content flex flex-col">
    <!-- Navbar -->
    {%@ 'navbar' %}
    
    <!-- Page content here -->
    <div class="md:container md:max-w-5xl md:mx-auto px-1 sm:px-2">
      <main class="min-h-screen px-3">
        {{ content }}
      </main>

    {% if page_index %}
      {%@ 'bottom_nav', prev_pg: prev_pg, next_pg: next_pg %}
    {% end %}

    {%@ 'footer', metadata: site.metadata %}
  </div>

  </div>
  <div class="drawer-side">
    <label for="my-drawer-3" aria-label="close sidebar" class="drawer-overlay"></label>
    {%@ 'menu' %}
  </div>
</div>