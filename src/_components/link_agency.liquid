{% assign agency = pages.agencies[id] %}

{% if agency and agency.path != 'none' %}
  <a href="{{ agency.path }}"><span class="has-text-weight-bold"><abbr title="{{agency.name}}">{{ agency.id }}</agency></span></a>
{% else %}
  <span class="has-text-weight-bold">{{ id }}</span>
{% endif %}
