<h1 class="title is-family-secondary is-size-2">{{ agency.name }}</h1>

{% if agency.children.any? %}
<h2 class="subtitle is-family-secondary is-5">subagencies: {{ agency.children | map: ->(a) { a.short_name } | sort | join: ", " }}</h2>
{% end %}
