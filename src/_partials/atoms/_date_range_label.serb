{% always_dash = false if always_dash.nil? %}

{% if first || last %}<span class="md:text-nowrap">{{ render EdtfFormat.new(first, :compact, :none) }}{% if always_dash || last %}-{% end %}{%if last %}{{ render EdtfFormat.new(last, :compact) }}{% end %}</span>{% end %}